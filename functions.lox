
fun add_one(n) {
    return n + 1;
}

fun concat(x, y) {
    return x + y;
}

fun sum(n) {
    var x = 0;
    
    for (var i = 0; i < n + 1; i = i + 1) {
        x = x + i;
    }

    return x;
}

print add_one(10);
print concat("hello ", "world!");
print sum(10);